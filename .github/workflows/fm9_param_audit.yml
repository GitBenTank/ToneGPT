name: FM9 Parameter Audit

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]

jobs:
  param_audit:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run FM9 Parameter Audit
      run: |
        python fm9_param_audit.py \
          --blocks data/blocks_with_footswitch.json \
          --cfg data/fm9_config.json \
          --ref data/fm9_comprehensive_reference.json \
          --out report/fm9_param_audit.md
          
    - name: Upload audit report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: fm9-param-audit-report
        path: report/fm9_param_audit.md
        
    - name: Comment PR with audit results
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const path = require('path');
          
          const reportPath = 'report/fm9_param_audit.md';
          if (fs.existsSync(reportPath)) {
            const report = fs.readFileSync(reportPath, 'utf8');
            
            // Extract compliance status
            const complianceMatch = report.match(/## (✅|❌) Compliance Status: (PASSED|FAILED)/);
            const status = complianceMatch ? complianceMatch[2] : 'UNKNOWN';
            
            const comment = `## 🔍 FM9 Parameter Audit Results
            
            **Status:** ${status}
            
            <details>
            <summary>View full audit report</summary>
            
            \`\`\`markdown
            ${report}
            \`\`\`
            
            </details>
            
            ${status === 'FAILED' ? '❌ **This PR cannot be merged until all parameter audit issues are resolved.**' : '✅ **All parameters are properly sourced from FM9 reference files.**'}
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });
          }
          
    - name: Fail if audit failed
      if: failure()
      run: |
        echo "❌ FM9 Parameter Audit failed. Please fix all compliance issues before merging."
        echo "Check the audit report for details on missing parameters and other issues."
        exit 1
